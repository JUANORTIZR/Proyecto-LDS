import { FiltroMovilidadPipe } from './filtro-movilidad.pipe';

describe('FiltroMovilidadPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroMovilidadPipe();
    expect(pipe).toBeTruthy();
  });
});
