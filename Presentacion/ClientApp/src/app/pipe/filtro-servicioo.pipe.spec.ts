import { FiltroServiciooPipe } from './filtro-servicioo.pipe';

describe('FiltroServiciooPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroServiciooPipe();
    expect(pipe).toBeTruthy();
  });
});
