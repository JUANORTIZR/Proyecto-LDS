import { FiltroUtilidadPipe } from './filtro-utilidad.pipe';

describe('FiltroUtilidadPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroUtilidadPipe();
    expect(pipe).toBeTruthy();
  });
});
